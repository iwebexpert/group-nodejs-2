const config = {
    db: {
        host: 'localhost',
        database: 'node',
        user: 'node',
        password: 'node',
        connectionLimit: 3,
        waitForConnections: true,
    }
}
module.exports = config;